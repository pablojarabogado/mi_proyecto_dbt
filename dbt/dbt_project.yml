name: 'maven_fuzzy_factory'
version: '1.0.0'
config-version: 2

profile: 'maven_fuzzy_factory'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  maven_fuzzy_factory:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
